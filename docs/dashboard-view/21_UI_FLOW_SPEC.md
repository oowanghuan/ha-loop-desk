# 21_UI_FLOW_SPEC.md
# Dashboard View - UI 流程规格

> 版本：v1.0
> 最后更新：2024-12-30
> 状态：Draft
> 负责人：AI PE

---

## 1. 概述

Dashboard View 是 Coding GUI 的全局视图层，位于 WorkspaceView 之上，提供多 Feature 的进度总览和快速导航。

### 1.1 导航层级

```
┌─────────────────────────────────────────────────────────────┐
│  DashboardView (新增)                                       │
│  ├── 所有 Feature 的甘特图总览                               │
│  ├── Daily Standup 汇总                                     │
│  └── 点击 Feature → 进入 WorkspaceView                      │
└─────────────────────────────────────────────────────────────┘
                           ↓ 点击 Feature
┌─────────────────────────────────────────────────────────────┐
│  WorkspaceView (现有)                                       │
│  ├── 单个 Feature 的 Phase 详情                             │
│  ├── 执行清单                                               │
│  └── 返回按钮 → 回到 DashboardView                          │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 路由配置

| 路由 | 视图 | 描述 |
|------|------|------|
| `/` | DashboardView | 默认首页，Feature 总览 |
| `/workspace/:featureId` | WorkspaceView | 单 Feature 工作区 |
| `/workspace/:featureId/:phaseId` | WorkspaceView | 指定 Phase |

---

## 2. 页面布局

### 2.1 DashboardView 整体结构

```
┌─────────────────────────────────────────────────────────────┐
│  Header (复用现有)                                    50px  │
│  ├── Logo + 项目名称                                        │
│  ├── 当前视图: "项目看板"                                   │
│  └── 设置按钮                                               │
├─────────────────────────────────────────────────────────────┤
│  Daily Standup Panel                                 120px  │
│  ├── 今日进展 (Highlights)                                  │
│  ├── 阻塞问题 (Blockers) - 红色高亮                         │
│  └── 明日计划 (Tomorrow)                                    │
├─────────────────────────────────────────────────────────────┤
│  Toolbar                                              44px  │
│  ├── 刷新按钮                                               │
│  ├── 视图切换: 甘特图 | 卡片 | 列表                         │
│  └── 筛选: 全部 | 进行中 | 已完成 | 阻塞                    │
├─────────────────────────────────────────────────────────────┤
│  Main Content Area                                   剩余   │
│  ├── [甘特图视图] Feature 行 + Phase 进度条                 │
│  ├── [卡片视图] Feature 卡片网格                            │
│  └── [列表视图] Feature 表格                                │
├─────────────────────────────────────────────────────────────┤
│  StatusBar (复用现有)                                 44px  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 尺寸定义

```css
:root {
  --header-height: 50px;
  --standup-panel-height: 120px;
  --toolbar-height: 44px;
  --status-bar-height: 44px;
  --content-height: calc(100vh - 50px - 120px - 44px - 44px);
}
```

---

## 3. 组件规格

### 3.1 DailyStandupPanel

**职责**：显示今日工作汇总

```
┌─────────────────────────────────────────────────────────────┐
│ 🤖 AI Project Agent · Daily Standup          [Refresh Plan] │
├──────────────────┬──────────────────┬───────────────────────┤
│ ✨ 今日进展       │ ⚠️ 阻塞问题       │ 📅 明日计划           │
│ • Feature A:     │ • Feature B:     │ • 完成 Feature A     │
│   Spec 完成      │   等待 API 对接   │   的 Code 阶段       │
│ • Feature C:     │                  │ • 开始 Feature D     │
│   Design 审批    │                  │                      │
└──────────────────┴──────────────────┴───────────────────────┘
```

**Props**:
```typescript
interface DailyStandupPanelProps {
  highlights: StandupItem[]    // 今日进展
  blockers: BlockerItem[]      // 阻塞问题
  tomorrow: PlanItem[]         // 明日计划
  onRefresh: () => void        // 刷新回调
}

interface StandupItem {
  featureId: string
  featureName: string
  summary: string              // 例如 "Spec 阶段完成"
}

interface BlockerItem {
  featureId: string
  issue: string
  blockedSince: string         // ISO date
}

interface PlanItem {
  plan: string
}
```

**数据来源**：
- 从所有 Feature 的 `90_PROGRESS_LOG.yaml` 提取
- `highlights`: 今日 `completed_at` 的任务
- `blockers`: `status: blocked` 的任务
- `tomorrow`: 下一个 `status: pending` 的任务

---

### 3.2 GanttChart

**职责**：以甘特图形式展示所有 Feature 的进度

```
┌─────────────────────────────────────────────────────────────────────┐
│ Feature 名称        │ 12/25  12/26  12/27  12/28  12/29  12/30     │
├─────────────────────┼───────────────────────────────────────────────┤
│ ▼ dashboard-view    │        ██████ Kickoff                        │
│   Phase 1 Kickoff   │        ██████████████                        │
│   Phase 2 Spec      │                      ██████████ (当前)       │
│   Phase 4 Design    │                                ░░░░░░        │
│   Phase 5 Code      │                                      ░░░░░░░ │
├─────────────────────┼───────────────────────────────────────────────┤
│ ► coding-GUI        │ ████████████████████████████████████ 100%    │
├─────────────────────┼───────────────────────────────────────────────┤
│ ► TSP-pricing       │ ██████████████████████░░░░░░░░░░░░░░ 65%     │
└─────────────────────┴───────────────────────────────────────────────┘

图例: ████ 已完成  ████ 进行中  ░░░░ 待开始  ████ 阻塞
```

**Props**:
```typescript
interface GanttChartProps {
  features: GanttFeature[]
  dateRange: DateRange
  onFeatureClick: (featureId: string) => void
  onPhaseClick: (featureId: string, phaseId: number) => void
}

interface GanttFeature {
  id: string
  name: string
  status: 'wip' | 'done' | 'blocked' | 'pending'
  progress: number              // 0-100
  phases: GanttPhase[]
  collapsed: boolean            // 是否收起
}

interface GanttPhase {
  phaseId: number               // 1-7
  phaseName: string             // "Kickoff", "Spec", etc.
  status: 'done' | 'wip' | 'pending' | 'blocked' | 'skipped'
  progress: number              // 0-100
  startDate?: string            // ISO date
  endDate?: string              // ISO date
}

interface DateRange {
  start: string                 // ISO date
  end: string                   // ISO date
}
```

**交互**:
| 操作 | 行为 |
|------|------|
| 点击 Feature 名称 | 跳转到 WorkspaceView |
| 点击展开/收起图标 | 切换 Phase 行显示 |
| 点击 Phase 条 | 跳转到 WorkspaceView 指定 Phase |
| 悬停 Phase 条 | 显示 Tooltip（进度详情） |

---

### 3.3 FeatureCard

**职责**：卡片视图中的单个 Feature 卡片

```
┌─────────────────────────────────────┐
│ dashboard-view                 WIP  │
│ Dashboard View 总览视图             │
├─────────────────────────────────────┤
│ Phase 2: Spec                  35%  │
│ ████████████░░░░░░░░░░░░░░░░░░░░░░ │
├─────────────────────────────────────┤
│ 1 ✓  2 ●  3 -  4 ○  5 ○  6 ○  7 ○  │
│ (Phase 进度指示器)                  │
├─────────────────────────────────────┤
│ 最近更新: 2024-12-30               │
│                        [进入工作区] │
└─────────────────────────────────────┘
```

**Props**:
```typescript
interface FeatureCardProps {
  feature: FeatureInfo
  onClick: () => void
}

interface FeatureInfo {
  id: string
  name: string
  description?: string
  currentPhase: number
  currentPhaseName: string
  status: 'wip' | 'done' | 'blocked' | 'pending'
  progress: number
  phases: PhaseStatus[]
  lastUpdated: string
}

interface PhaseStatus {
  phaseId: number
  status: 'done' | 'wip' | 'pending' | 'blocked' | 'skipped'
}
```

---

### 3.4 Toolbar

**职责**：视图切换和筛选

```
┌─────────────────────────────────────────────────────────────┐
│ [🔄 刷新]   视图: [甘特图|卡片|列表]   筛选: [全部 ▼]  搜索: [    ] │
└─────────────────────────────────────────────────────────────┘
```

**Props**:
```typescript
interface ToolbarProps {
  viewMode: 'gantt' | 'card' | 'list'
  filter: 'all' | 'wip' | 'done' | 'blocked'
  searchQuery: string
  onViewModeChange: (mode: string) => void
  onFilterChange: (filter: string) => void
  onSearch: (query: string) => void
  onRefresh: () => void
}
```

---

## 4. 数据流

### 4.1 数据获取流程

```
┌─────────────────────────────────────────────────────────────┐
│  useDashboard() composable                                  │
│  ├── 调用 Schema Discovery 扫描 docs/ 目录                  │
│  ├── 识别所有 Feature（含 90_PROGRESS_LOG.yaml 的目录）     │
│  ├── 解析各 Feature 的 Phase 状态                          │
│  └── 聚合为 Dashboard 数据结构                              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  DashboardView                                              │
│  ├── DailyStandupPanel                                      │
│  ├── Toolbar                                                │
│  └── GanttChart / FeatureCard / ListView                    │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 IPC 调用

```typescript
// 新增 IPC channel
interface DashboardIPC {
  // 获取所有 Feature 列表
  'dashboard:getFeatures': () => Promise<FeatureInfo[]>

  // 获取单个 Feature 详情
  'dashboard:getFeatureDetail': (featureId: string) => Promise<FeatureDetail>

  // 刷新数据
  'dashboard:refresh': () => Promise<void>
}
```

### 4.3 状态管理

```typescript
// stores/dashboard.store.ts
interface DashboardState {
  features: FeatureInfo[]
  loading: boolean
  error: string | null
  viewMode: 'gantt' | 'card' | 'list'
  filter: 'all' | 'wip' | 'done' | 'blocked'
  searchQuery: string
  expandedFeatures: Set<string>
}
```

---

## 5. 交互流程

### 5.1 启动流程

```
1. 用户启动 Coding GUI
2. 默认路由到 DashboardView (/)
3. DashboardView 挂载
4. 调用 useDashboard().loadFeatures()
5. Schema Discovery 扫描 docs/ 目录
6. 解析所有 90_PROGRESS_LOG.yaml
7. 渲染 Dashboard 内容
```

### 5.2 进入 Feature 详情

```
1. 用户在 Dashboard 点击 Feature 名称/卡片
2. Router 导航到 /workspace/:featureId
3. WorkspaceView 加载指定 Feature
4. （可选）用户点击"返回看板"回到 Dashboard
```

### 5.3 刷新数据

```
1. 用户点击"刷新"按钮
2. 调用 dashboard:refresh IPC
3. 重新扫描 docs/ 目录
4. 更新 UI
```

---

## 6. 错误处理

| 场景 | 处理方式 |
|------|----------|
| 无 Feature 目录 | 显示空状态：「暂无功能模块，使用 /new-feature 创建」 |
| YAML 解析失败 | 跳过该 Feature，显示警告图标 |
| 扫描超时 | 显示错误提示，提供重试按钮 |
| Feature 目录被删除 | 下次刷新时自动移除 |

---

## 7. 边界条件

| 条件 | 处理 |
|------|------|
| Feature 数量 > 50 | 启用虚拟滚动 |
| Feature 名称过长 | 截断显示，悬停显示完整名称 |
| Phase 无日期信息 | 甘特图条显示在当前日期位置 |
| 同时多个 Feature 阻塞 | Standup 面板最多显示 5 个，其余折叠 |

---

## 8. 视觉规范

### 8.1 状态颜色（复用 GitHub Dark 主题）

| 状态 | 颜色 | 用途 |
|------|------|------|
| Done | `#3fb950` 绿色 | 已完成 Phase/Feature |
| WIP | `#58a6ff` 蓝色 | 进行中 |
| Pending | `#6e7681` 灰色 | 待开始 |
| Blocked | `#f85149` 红色 | 阻塞 |
| Skipped | `#6e7681` 灰色虚线 | 跳过 |

### 8.2 甘特图条样式

```css
/* 已完成 */
.gantt-bar--done {
  background: linear-gradient(90deg, #238636, #3fb950);
  border-radius: 4px;
}

/* 进行中 */
.gantt-bar--wip {
  background: linear-gradient(90deg, #1f6feb, #58a6ff);
  border-radius: 4px;
}

/* 待开始 */
.gantt-bar--pending {
  background: #30363d;
  border: 1px dashed #6e7681;
  border-radius: 4px;
}

/* 阻塞 */
.gantt-bar--blocked {
  background: linear-gradient(90deg, #da3633, #f85149);
  border-radius: 4px;
}
```

---

## 9. 与现有组件的关系

| 现有组件 | 复用/修改 |
|----------|----------|
| Header | 复用，修改标题显示 |
| StatusBar | 复用 |
| PhaseNav | 不复用（Dashboard 有自己的布局） |
| WorkspaceView | 作为跳转目标，增加返回按钮 |

---

## 10. 文件结构（预期）

```
src/renderer/
├── views/
│   ├── DashboardView.vue          # 新增
│   └── WorkspaceView.vue          # 修改（增加返回按钮）
├── components/
│   ├── dashboard/                  # 新增目录
│   │   ├── DailyStandupPanel.vue
│   │   ├── GanttChart.vue
│   │   ├── FeatureCard.vue
│   │   ├── FeatureRow.vue
│   │   └── DashboardToolbar.vue
│   └── ...
├── composables/
│   └── useDashboard.ts            # 新增
├── stores/
│   └── dashboard.store.ts         # 新增
└── router/
    └── index.ts                   # 修改路由配置
```

---

## CHANGELOG

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| v1.0 | 2024-12-30 | AI PE | 初始版本 |
